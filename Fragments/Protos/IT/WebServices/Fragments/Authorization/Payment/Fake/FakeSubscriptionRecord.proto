syntax = "proto3";

package IT.WebServices.Fragments.Authorization.Payment.Fake;

import "google/protobuf/timestamp.proto";

// User claim's record
message FakeSubscriptionRecord {
  string UserID = 1; // Guid for the user
  uint32 AmountCents = 2;
  google.protobuf.Timestamp ChangedOnUTC = 3;
}
